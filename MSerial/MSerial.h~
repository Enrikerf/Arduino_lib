#ifndef MSerial_h
#define MSerial_h
#include"Arduino.h"
class MSerial{
  public:
    MSerial();
    MSerial(int Tam); // Tam va a ser la cantidad de caracteres ascii que es capaz de leer de golpe    
    int GetSerialNum(float * DatosDepurados);// apuntará a la cantidad de datos que se han obtenido
    ~MSerial();
private:  
    short int NDatDep;// para la funcion de invertir 
    float *DatosDep;
    int MaxTamBuffer,*Datos;
    int MBuffer; 
//---------------------------------------------------
    void DepDatNum();// Convierte a float los datos del cogido ASCII adquirido por Pto Serie
    void InvertirDat();// los datos se depuran al reves por lo que hay que invertirlos
    double pow(double base, double exponente);   
       
};
#endif
